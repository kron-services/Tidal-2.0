export interface Apps{
    name: string
    pkg: number
    description: string
    version?: number
    author?: string
};

export interface wm{
    size?: number
    width?: number
    name: string
}
